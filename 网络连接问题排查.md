# GitHub 网络连接问题排查指南

**问题症状**：`git push` 报错 `Failed to connect to github.com port 443`

## ⚡ 快速修复（3 步）

### 方案 1：配置代理（推荐）

如果你已安装代理工具（Clash、V2Ray 等），按以下步骤配置：

```bash
# 运行代理配置脚本（自动化）
powershell -ExecutionPolicy Bypass -File "配置Git代理.ps1"

# 或手动配置（以 Clash 端口 7890 为例）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890
```

然后运行推送脚本：
```bash
powershell -ExecutionPolicy Bypass -File "推送v1.5.0到GitHub.ps1"
```

---

### 方案 2：测试 DNS（如果代理也不行）

检查 DNS 解析是否正常：

```bash
# 测试 DNS 解析
nslookup github.com

# 应该显示类似：
# Server: 8.8.8.8
# Address: 8.8.8.8
# github.com   canonical name = github.com.
# Address: 20.205.243.166
```

如果 DNS 无法解析，尝试改用国内 DNS：
```bash
# Windows 设置 DNS 为 114.114.114.114 或 8.8.8.8
# 设置路径：
# 控制面板 > 网络和共享中心 > 更改适配器设置 > 网络属性 > IPv4 属性
```

---

### 方案 3：检查防火墙

检查 Windows 防火墙是否阻止了 Git：

```bash
# 查看防火墙规则
Get-NetFirewallRule -DisplayName *git* -ErrorAction SilentlyContinue

# 如果 Git 被阻止，临时关闭防火墙
netsh advfirewall set allprofiles state off

# 完成后重新开启
netsh advfirewall set allprofiles state on
```

---

## 🔍 诊断命令

### 1. 检查网络连接

```bash
# Ping GitHub（检查基础网络）
ping github.com

# 预期：能正常 Ping 通（延迟 > 100ms）
```

### 2. 检查 443 端口连接

```bash
# Windows PowerShell
Test-NetConnection -ComputerName github.com -Port 443

# 预期输出：TcpTestSucceeded: True
```

如果失败，说明：
- ✅ 防火墙阻止了 HTTPS 443 端口
- ✅ 需要代理才能连接

### 3. 测试 HTTP 与 HTTPS 区别

```bash
# 测试 HTTP（80 端口）
Test-NetConnection -ComputerName github.com -Port 80

# 如果 80 通但 443 不通，说明需要代理
```

### 4. 尝试改用 SSH

```bash
# 切换到 SSH 地址
git remote set-url origin git@github.com:00OO666/fangyu-code.git

# 测试 SSH 连接
ssh -T git@github.com

# 注意：需要已配置 SSH 密钥
```

---

## 🛠️ 详细配置指南

### 配置代理的几种方式

#### 方式 1：全局配置（推荐）
```bash
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890
```

#### 方式 2：仅为 GitHub 配置
```bash
git config --global http.https://github.com.proxy http://127.0.0.1:7890
```

#### 方式 3：项目级配置（仅在此项目生效）
```bash
cd F:\Any-Code-Dev
git config http.proxy http://127.0.0.1:7890
git config https.proxy http://127.0.0.1:7890
```

#### 查看已配置的代理
```bash
# 全局代理
git config --global --list | findstr proxy

# 项目代理
git config --local --list | findstr proxy
```

#### 删除代理配置
```bash
# 删除全局代理
git config --global --unset http.proxy
git config --global --unset https.proxy

# 删除项目代理
git config --unset http.proxy
git config --unset https.proxy
```

---

## 📊 常见代理工具端口

| 工具 | 类型 | 端口 | 配置 |
|------|------|------|------|
| **Clash** | HTTP | 7890 | `http://127.0.0.1:7890` |
| Clash SOCKS5 | SOCKS5 | 7891 | N/A（Git 不支持 SOCKS5） |
| **V2Ray** | SOCKS5 | 10809 | N/A（需要 HTTP） |
| Shadowsocks | SOCKS5 | 1080 | N/A |
| **Proxifier** | HTTP | 8888 | `http://127.0.0.1:8888` |
| **Trojan** | SOCKS5 | 1080 | N/A |

⚠️ **注意**：Git 默认只支持 HTTP 代理，如果你用的是 SOCKS5 代理，需要使用 Privoxy 或 Polipo 转换。

---

## 🚀 快速测试推送

配置完代理后，运行此命令测试：

```bash
# 方式 1：使用推送脚本（推荐）
powershell -ExecutionPolicy Bypass -File "推送v1.5.0到GitHub.ps1"

# 方式 2：手动推送
cd F:\Any-Code-Dev
git push origin main      # 推送代码
git push origin v1.5.0    # 推送标签（触发 GitHub Actions）
```

---

## 📋 推送成功的标志

✅ 推送成功时，输出应该包含：
```
Counting objects: 5, done.
Compressing objects: 100% (3/3), done.
Writing objects: 100% (5/5), 1.23 KiB | 512.00 KiB/s, done.
Total 5 (delta 0), reused 0 (delta 0), pack-reused 0
remote: Resolving deltas: 100% (0/0), done.
To https://github.com/00OO666/fangyu-code.git
   05f8ee2..3783d3a  main -> main
```

❌ 推送失败时，报错应该包含：
```
fatal: unable to access 'https://github.com/...':
Failed to connect to github.com port 443
```

---

## 🎯 下一步

推送成功后：

1. **监控 GitHub Actions 构建**
   - 访问：https://github.com/00OO666/fangyu-code/actions
   - 等待 Tauri 构建完成（约 5-10 分钟）

2. **查看 Release**
   - 访问：https://github.com/00OO666/fangyu-code/releases
   - 应该看到新的 v1.5.0 Release

3. **验证应用更新**
   - Fangyu Code 会自动检测新版本
   - 点击"更新"会下载并安装 v1.5.0

---

## ❓ 常见问题

**Q: 我没有代理工具，怎么办？**
A: 可以：
- 尝试方案 2（修改 DNS）
- 或使用 GitHub Desktop 应用（自带网络配置）
- 或使用国内 GitHub 镜像（如 Gitee）

**Q: 配置了代理还是不行？**
A: 检查：
- 代理工具是否已启动并正在运行
- 代理端口是否正确（运行 `netstat -ano | findstr 7890`）
- 是否需要代理认证（用户名/密码）

**Q: SSH 能连接，HTTPS 不行？**
A: 这说明：
- 防火墙阻止了 HTTPS（443 端口）
- 需要配置代理才能 HTTPS 连接
- 可以尝试使用 SSH 推送：`git remote set-url origin git@github.com:00OO666/fangyu-code.git`

---

## 🔗 相关资源

- [Git 官方代理配置](https://git-scm.com/book/zh/v2/%E8%B5%B7%E6%AD%A5-%E6%9C%80%E5%88%9D%E7%9A%84%E9%85%8D%E7%BD%AE)
- [GitHub 官方帮助](https://docs.github.com/zh/get-started/getting-started-with-git)
- [测试 SSH 连接](https://docs.github.com/zh/authentication/connecting-to-github-with-ssh/testing-your-ssh-connection)

---

**最后更新**: 2026-01-01
**创建者**: Claude Code
